use crate::day03::{Input, Output};

// REFACTOR: after input only shows chars
pub fn solve(input: &Input) -> Output {
    let sum_of_priorities: u128 = 0;

    // Iterate through input, looking at three packs at a time
    // Find element carried by all three
    // Add to sum of priorities

    sum_of_priorities.into()
}
